{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "lldb",
			"request": "launch",
			"name": "Launch and debug",
			"program": "${workspaceFolder}\\main.exe",

			"cwd": "${workspaceFolder}",
			"preLaunchTask": "Build cpp"
		}
	]
}